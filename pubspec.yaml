name: sekebit
description: sekebit webapp.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  pull_to_refresh: ^2.0.0
  local_auth: ^2.1.6
  hive: ^2.2.3
  webview_flutter: ^4.0.7
  go_router: ^6.0.0
  flutter_hooks: ^0.18.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data.json
